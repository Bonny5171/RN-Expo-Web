import React from 'react';
import { View, Text } from 'react-native';

const ProductView = () => {
  <View style={{ backgroundColor: 'transparent', height: 400 }}>
    <Text>
      Box preview do produto.
    </Text>
  </View>
}

export default ProductView;